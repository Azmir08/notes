# NPM package install in backend

- bcrypt
- cors
- dotenv
- express
- jsonwebtoken
- mongoose
- nodemon

# Generate access token in nodejs

- node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
